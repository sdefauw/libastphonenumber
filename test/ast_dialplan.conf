[test]
exten => _X.,1,NoOp(TEST libastphonenumber)
exten => _X.,n,NoOp(${CALLERID(num)} -> ${EXTEN})
exten => _X.,n,NoOp(FORMATTING)
exten => _X.,n,Set(numpluse164=${FORMAT_NUM(${EXTEN}:BE:+e164)})
exten => _X.,n,Set(nume164=${FORMAT_NUM(${EXTEN}:BE:e164)})
exten => _X.,n,Set(numenat=${FORMAT_NUM(${EXTEN}:BE:nat)})
exten => _X.,n,Set(numenatshort=${FORMAT_NUM(${EXTEN}:BE:nat_short)})
exten => _X.,n,Set(numeext=${FORMAT_NUM(${EXTEN}:GB:ext)})
exten => _X.,n,NoOp(VALIDITY OF THE NUMBER)
exten => _X.,n,Set(validnum=${IS_VALID_NUM(${EXTEN}:BE)})
exten => _X.,n,Set(validnum=${IS_VALID_NUM(300:BE)})
exten => _X.,n,Set(validnum=${IS_VALID_NUM(${EXTEN}:GB:l)})
exten => _X.,n,NoOp(NUMBER INFORMATION)
exten => _X.,n,Set(ccc=${NUM_INFO(${EXTEN}:BE:ccc)})
exten => _X.,n,Set(country=${NUM_INFO(${EXTEN}:BE:country)})
exten => _X.,n,Set(ton=${NUM_INFO(${EXTEN}:BE:type)})
exten => _X.,n,NoOp(REGION INFORMATION)
exten => _X.,n,Set(ccc_be=${REGION_INFO(BE:ccc)})
exten => _X.,n,Set(ccc_be=${REGION_INFO(US:ccc)})
exten => _X.,n,Set(ccc_be=${REGION_INFO(GB:ccc)})