[test]
exten => _X.,1,NoOp(=====>TEST<=======)
exten => _X.,n,NoOp(${CALLERID(num)} -> ${EXTEN})
exten => _X.,n,Set(numpluse164=${FORMATNUM(${EXTEN}:BE:+e164)})
exten => _X.,n,Set(nume164=${FORMATNUM(${EXTEN}:BE:e164)})
exten => _X.,n,Set(numenat=${FORMATNUM(${EXTEN}:BE:nat)})
exten => _X.,n,Set(numenatshort=${FORMATNUM(${EXTEN}:BE:nat_short)})
exten => _X.,n,Set(numeext=${FORMATNUM(${EXTEN}:GB:ext)})
exten => _X.,n,NoOp(--- VALIDNUM ---)
exten => _X.,n,Set(validnum=${VALIDNUM(${EXTEN}:BE)})
exten => _X.,n,Set(validnum=${VALIDNUM(300:BE)})
exten => _X.,n,Set(validnum=${VALIDNUM(${EXTEN}:GB:l)})
exten => _X.,n,NoOp(--- INFONUM ---)
exten => _X.,n,Set(ccc=${INFONUM(${EXTEN}:BE:ccc)})
exten => _X.,n,Set(country=${INFONUM(${EXTEN}:BE:country)})
exten => _X.,n,Set(ton=${INFONUM(${EXTEN}:BE:type)})
exten => _X.,n,NoOp(--- INFOREGIONM ---)
exten => _X.,n,Set(ccc_be=${INFOREGION(BE:ccc)})
exten => _X.,n,Set(ccc_be=${INFOREGION(US:ccc)})
exten => _X.,n,Set(ccc_be=${INFOREGION(GB:ccc)})xt)})